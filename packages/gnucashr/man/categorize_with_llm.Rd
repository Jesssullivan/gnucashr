% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm-categorize.R
\name{categorize_with_llm}
\alias{categorize_with_llm}
\title{Categorize transactions using an LLM}
\usage{
categorize_with_llm(
  descriptions,
  account_tree,
  provider = c("anthropic", "openai"),
  model = NULL,
  token = NULL,
  batch_size = 20L,
  min_confidence = 0.5
)
}
\arguments{
\item{descriptions}{Character vector of transaction descriptions to categorize.}

\item{account_tree}{Character vector of full account paths from the book
(e.g., \code{"Expenses:SaaS:AI"}).}

\item{provider}{LLM provider: \code{"anthropic"} or \code{"openai"}.}

\item{model}{Model identifier. Defaults to provider-specific model.}

\item{token}{API key. If \code{NULL}, resolved from environment variables
or keyring.}

\item{batch_size}{Number of descriptions per API call (default 20).}

\item{min_confidence}{Minimum confidence threshold (0-1). Results below
this are marked \code{"unknown"}.}
}
\value{
A data frame with columns: \code{description}, \code{category},
\code{confidence}, \code{reasoning}.
}
\description{
Sends batches of transaction descriptions to an LLM API and returns
suggested account categorizations with confidence scores.
}
\details{
Requires \pkg{httr2} (in Suggests). Returns an error if \pkg{httr2} is not
installed.
}
