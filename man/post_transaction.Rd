% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-transaction.R
\name{post_transaction}
\alias{post_transaction}
\title{Post a Transaction}
\usage{
post_transaction(
  gc,
  description,
  splits,
  post_date = Sys.Date(),
  num = NULL,
  currency = NULL
)
}
\arguments{
\item{gc}{GnuCashDB object (must be opened with read_only = FALSE)}

\item{description}{Transaction description}

\item{splits}{List of splits, each containing:
\itemize{
\item account: Account GUID or path
\item value: Numeric value (positive = debit, negative = credit)
\item memo: Optional memo
\item action: Optional action
}}

\item{post_date}{Transaction date (default: today)}

\item{num}{Optional transaction number/reference}

\item{currency}{Currency mnemonic (default: book default)}
}
\value{
Result with new transaction GUID or error
}
\description{
Create a new transaction with splits in GnuCash.
}
\examples{
\dontrun{
# Simple two-split transaction
post_transaction(gc, "Office supplies",
  splits = list(
    list(account = "Expenses:Office", value = 50.00),
    list(account = "Assets:Checking", value = -50.00)
  )
)

# Multi-split transaction
post_transaction(gc, "Payroll",
  splits = list(
    list(account = "Expenses:Salary", value = 5000),
    list(account = "Expenses:Payroll Tax", value = 382.50),
    list(account = "Liabilities:Payroll Tax", value = -382.50),
    list(account = "Assets:Checking", value = -5000)
  )
)
}
}
