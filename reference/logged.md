# Create Logged Operation Result

Wrap a value with an audit log entry.

## Usage

``` r
logged(value, log_entries = character())
```

## Arguments

- value:

  The result value

- log_entries:

  Character vector of log entries

## Value

A logged result object
