# Collect Forecast Results

Execute the lazy forecast and return materialized results.

## Usage

``` r
lf_collect(lf, parallel = TRUE)
```

## Arguments

- lf:

  LazyForecast object

- parallel:

  Use parallel execution (default TRUE)

## Value

Materialized forecast results
