# Create a new LazyForecast

Factory function for creating LazyForecast objects.

## Usage

``` r
lazy_forecast(source = NULL)
```

## Arguments

- source:

  Data source (config, tibble, GnuCashDB, BookCollection)

## Value

LazyForecast object
