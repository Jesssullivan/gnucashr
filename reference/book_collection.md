# Create a Book Collection

Convenience function to create a BookCollection instance.

## Usage

``` r
book_collection(entity_config = NULL)
```

## Arguments

- entity_config:

  Optional entity configuration

## Value

A BookCollection object
