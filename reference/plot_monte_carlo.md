# Create Monte Carlo Fan Chart

Creates a ggplot2 fan chart showing Monte Carlo simulation results.

## Usage

``` r
plot_monte_carlo(mc_result, title = NULL)
```

## Arguments

- mc_result:

  Monte Carlo result from lf_collect() or quick_monte_carlo()

- title:

  Optional plot title

## Value

A ggplot2 object
