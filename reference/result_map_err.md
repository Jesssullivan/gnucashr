# Map Over Error

Apply a function to the error if Err, pass through if Ok.

## Usage

``` r
result_map_err(x, fn)
```

## Arguments

- x:

  A result object

- fn:

  Function to apply to error

## Value

New result with transformed error
