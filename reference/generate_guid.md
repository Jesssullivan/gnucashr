# Generate GnuCash-Format GUID

Generate a random 32-character hex GUID in GnuCash format. GnuCash uses
lowercase hex without dashes.

## Usage

``` r
generate_guid()
```

## Value

Character string with 32-character hex GUID
