# Safe Trial Balance

Generate trial balance with error handling.

## Usage

``` r
safe_trial_balance(gc, as_of = Sys.Date())
```

## Arguments

- gc:

  GnuCashDB object

- as_of:

  Date for balance

## Value

Result with trial balance or error
