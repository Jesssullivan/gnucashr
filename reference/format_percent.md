# Format Percentage for Display

Helper function to format percentage values for display.

## Usage

``` r
format_percent(x, digits = 1, suffix = "%")
```

## Arguments

- x:

  Numeric value (0.05 = 5%)

- digits:

  Decimal digits (default 1)

- suffix:

  Suffix (default "%")

## Value

Formatted character string
