# Validate Account GUID Format

Check if a string is a valid GnuCash GUID (32 hex characters).

## Usage

``` r
validate_guids(guids)
```

## Arguments

- guids:

  Character vector of GUIDs to validate

## Value

Logical vector indicating validity
