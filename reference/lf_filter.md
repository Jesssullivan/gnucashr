# Filter Forecast Data (Pipe-Friendly)

Add filter operation to a lazy forecast.

## Usage

``` r
lf_filter(lf, ...)
```

## Arguments

- lf:

  LazyForecast object

- ...:

  Filter conditions

## Value

LazyForecast (for chaining)
